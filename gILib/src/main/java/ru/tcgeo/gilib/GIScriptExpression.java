package ru.tcgeo.gilib;

public class GIScriptExpression {

	/*private String m_src;
	public GIScriptExpression() 
	{
		// TODO Auto-generated constructor stub
	}
	public void read(String src)
	{
		m_src = src;

		int index = 0; 
		while(index < m_src.length())
		{
			if(m_src.charAt(index) == '(')
			{
				ReadSection(index);			
			}
			index++;

		}
		int i = index;
	}
	private int ReadSection(int offset)
	{
		int os = offset;
		int braser_count = 1;
		String in_braser = "";
		while(braser_count != 0)
		{
			
			in_braser += m_src.charAt(os);
			os++;
			if(m_src.charAt(os) == '(')
			{
				braser_count++;
			}
			if(m_src.charAt(os) == ')')
			{
				braser_count--;
			}
		}

		
		return os;
	}*/
}
