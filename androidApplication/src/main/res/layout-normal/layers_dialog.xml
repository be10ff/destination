<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="Theme.Layer_Dialog_Padding"
    android:id="@+id/layers_dialog_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/layers_panel"
    android:orientation="vertical" 
    android:paddingLeft="15dp" 
    android:paddingRight="15dp" 
    android:paddingTop="50dp" 
    android:paddingBottom="30dp" >

    <!-- Top buttons general/additional -->
    <LinearLayout
        android:id="@+id/layers_top_buttons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal" 
        android:fadeScrollbars="false">

        <ImageButton
            android:id="@+id/layers_general_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:contentDescription="@string/layers_general_button"
            android:src="@drawable/layers_general_button" 
            android:layout_gravity="center"/>

        <ImageButton
            android:id="@+id/layers_additional_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:contentDescription="@string/layers_additional_button"
            android:src="@drawable/layers_additional_button_active" 
            android:layout_gravity="right"/>
    </LinearLayout>

    <ListView
        android:id="@+id/layers_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fadingEdgeLength="10dp"
        android:cacheColorHint="#00000000"
        android:background="@null"
        android:divider="@color/layers_dialog_dividers"
        android:dividerHeight="3dp" 
        android:layout_marginTop="10dp" />

</LinearLayout>